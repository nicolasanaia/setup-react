# Front

To install the packages, use:

```terminal
yarn install
```

To run the Dev environment, use:

```terminal
yarn dev
```
Then in your browser search for:

```browser
http://localhost:8080/
```
